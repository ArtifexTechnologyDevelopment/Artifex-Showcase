# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'
use_frameworks!
target 'Artifex-Showcase' do
# ignore all warnings from all pods
inhibit_all_warnings!
pod 'Kingfisher', '~> 4.0'
pod 'GoogleMaps'
end
